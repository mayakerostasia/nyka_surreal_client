// use nico_surreal_client::prelude::subscribe::subscribe;

#[tokio::main]
async fn main() -> Result<(), nico_surreal_client::Error> {
    // let subscribe = subscribe("thing").await?;

    // let _ = subscribe.await;
    Ok(())
}
